import React from 'react';
import PropTypes from 'prop-types';
import './Cards.css'

const Cards = (props) => {
    return (
        <div />
        );
};

Cards.propTypes = {
};

export default Cards;